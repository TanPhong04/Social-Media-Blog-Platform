spring:
  datasource:
    url: jdbc:h2:mem:notificationdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa.hibernate.ddl-auto: create-drop
  flyway.enabled: false
app:
  kafka.consumer-enabled: false
  security.jwt-secret: test-secret-that-is-definitely-at-least-thirty-two-bytes
