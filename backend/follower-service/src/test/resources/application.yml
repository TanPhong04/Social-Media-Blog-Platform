spring:
  datasource:
    url: jdbc:h2:mem:followerdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa.hibernate.ddl-auto: create-drop
  flyway.enabled: false
app:
  security:
    jwt:
      issuer: https://social-blog-platform.test
  kafka.consumer-enabled: false
  outbox.publisher-enabled: false
